def area(a, h):
    ```
    принимает два аргумента a и h - сторона и высота треугольника
    возвращает произведение a на h/2
    ```
    return a * h / 2

def perimeter(a, b, c):
    ```
    принимает три аргумента a, b, c - стороны треугольника
    возвращает сумму a, b, c
    ```
    return a + b + c
